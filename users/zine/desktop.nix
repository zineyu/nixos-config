# Host-specific overrides for the `desktop` machine.
#
# IMPORTANT: This is NOT the place for general "desktop" role configuration.
# Shared desktop settings belong in ../../modules/home/desktop. This module is
# only for overrides that apply to THIS SPECIFIC machine, e.g. display scaling,
# external monitor layout, hardware-specific packages, or per-machine toggles.
{ ... }:

{
  # Add host-specific settings here when needed.
}
